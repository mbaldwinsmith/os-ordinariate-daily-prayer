{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://personal-daily-prayer/schema/office-of-readings-day.schema.json",
  "title": "One day's Office of Readings (data/office-of-readings/<yearI|yearII>/<season>/weekN/<day>.json)",
  "type": "object",
  "required": ["scriptureReading", "verified"],
  "additionalProperties": false,
  "properties": {
    "verified": {
      "type": "boolean",
      "description": "false = a best-effort reconstruction pending cross-check against an authoritative lectionary index (see SOURCES.md); true once confirmed."
    },
    "scriptureReading": {
      "type": "object",
      "additionalProperties": false,
      "oneOf": [
        { "required": ["ref"], "properties": { "ref": {} } },
        { "required": ["refs"], "properties": { "refs": {} } }
      ],
      "properties": {
        "ref": { "type": "string", "minLength": 1, "description": "See CONVENTIONS.md reference syntax" },
        "refs": { "type": "array", "minItems": 2, "items": { "type": "string", "minLength": 1 }, "description": "Multiple references when one appointed reading crosses a biblical-book boundary." },
        "title": { "type": "string" }
      }
    },
    "patristicReading": {
      "description": "Nullable: Phase 7 allows omitting the second reading as an MVP fallback (Douay-Rheims has no patristic texts) - see TASKS.md Phase 7.",
      "type": ["object", "null"],
      "required": ["title"],
      "additionalProperties": false,
      "properties": {
        "title": { "type": "string", "minLength": 1 },
        "sourceRef": { "type": "string", "description": "Free-text citation, e.g. 'St Augustine, Confessions, Bk 1' - not a scripture reference" }
      }
    }
  }
}
